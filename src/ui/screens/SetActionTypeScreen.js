import { Text } from "native-base";
import React from "react";

const _SetActionTypeScreen = (props) => {
    return (
        <Text> "Add action screen" </Text>
    )
};

export default _SetActionTypeScreen;
